import * as a from '../../actions/actions_types.js'
const INITIAL_STATE = {};

export const {{camelCase actionName}}Reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
		case a.FILTER_{{constantCase actionName}}_{{constantCase scope}}:
			return (state = a.FILTER_{{constantCase actionName}}_{{constantCase scope}});

		case a...
			return

    default:
      return state;
  }
};

export default {{camelCase actionName}}Reducer;

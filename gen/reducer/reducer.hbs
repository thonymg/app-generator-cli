import * as types from './actions/{{camelCase actionName}}.actionTypes.js';
const INITIAL_STATE = {};

export const {{camelCase actionName}}Reducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
		case types.FILTER_{{constantCase actionName}}_{{constantCase scope}}:
			return (state = types.FILTER_{{constantCase actionName}}_{{constantCase scope}});

    default:
      return state;
  }
};

export default {{camelCase actionName}}Reducer;

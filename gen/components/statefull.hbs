import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

{{#if styles}}
import styles from './{{camelCase name}}.css';
{{/if}}

class {{pascalCase name}} extends React.Component {
  static propTypes = {};

  constructor(props) {
    super(props);

    this.state = {};
  }

	componentDidMount() {}

  render() {
    return (
      {{#if styles}}
      <Fragment className={styles.{{camelCase name ~}} }>
      {{else}}
      <Fragment>
      {{/if}}
        {{name}}
      </Fragment>
    );
  }
}

export default {{pascalCase name}};

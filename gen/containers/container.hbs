import { connect } from 'react-redux';

class {{pascalCase name}} extends React.Component {
  static propTypes = {};

  constructor(props) {
    super(props);

    this.state = {};
  }

	componentDidMount() {}

  render() {
    return (
      {{#if styles}}
      <Fragment className={styles.{{camelCase name ~}} }>
      {{else}}
      <Fragment>
      {{/if}}
        {{name}}
      </Fragment>
    );
  }
}

const mapStateToProps = (state, ownProps)  => ({});

const mapDispatchToProps = (dispatch, ownProps) => ({});

export default {{pascalCase name}}Container = connect(
  mapStateToProps,
  mapDispatchToProps,
)({{pascalCase name}});


